{
  "name": "node-package",
  "version": "1.0.0",
  "description": "Description for ENA CAF Library",
  "repository": "git@github.com:andyghiuta/node-package.git",
  "main": "dist/index.js",
  "scripts": {
    "compile": "babel src --out-dir dist",
    "build": "npm run test && npm run compile",
    "test": "nyc --require babel-register mocha --colors ./test/**/*.spec.js"
  },
  "files": [
    "dist",
    "**/*.less"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-minify": "^0.3.0",
    "chai": "^4.1.2",
    "mocha": "^4.0.1",
    "nyc": "^11.3.0"
  }
}
